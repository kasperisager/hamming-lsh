import test from 'ava';

test.todo('Table() constructs a lookup table for vectors of a given dimension');

test.todo('#size() returns the number of vectors in a lookup table');

test.todo('#add() adds a vector to a lookup table');

test.todo('#add() throws when adding a vector of a dimensionality different from the table');

test.todo('#query() queries a lookup table for an approximate nearest neighbour of a vector');

test.todo('#query() throws when querying a vector of a dimensionality different from the table');
